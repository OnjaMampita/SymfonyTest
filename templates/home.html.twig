{% extends 'base.html.twig' %}


{% block javascripts %}
    
        
 {% endblock %}

{% block body %}
   <style>
    body {
        background-color: #FFEBEE
    }
    
    h1 {
        
        text-align:center;
    }
    
    #bouton {
        margin-left: 200px;
        margin-top: 30px;
        width: 100px;
        height: 30px;
        font-weight: bold;
        background-color: grey;
        border-radius: 8px;
    }
    
    .card {
        /* width: 400px; */
        background-color: #fff;
        border: none;
        border-radius: 12px;
        max-width: 600px;
        margin-left: 400px;
    }
    
    
    
    .form-group {
        display: grid;
        /* grid-template-columns: max-content auto; */
        grid-template-columns: 20% auto;
        column-gap: 1em;
    }
    
    .form-group label {
        margin: auto 0;
    }
    
    .card-body {
        padding:10px;
    }
    .selecte{
        width:  575px;
        margin-right:200px;
        height:50px;
        border-radius:10px;
        font-size:30px;
        text-align:center;
    }
</style>



       

        <h1>OPTIONS</h1>

<div class="container mt-5 mb-5 d-flex justify-content-center">
    <div class="card px-1 py-4">
        <div class="card-body">
                 <FORM>
                    <SELECT class="selecte" NAME="ListeUrl" SIZE=1 onChange="ChangeUrl(this.form)">
                        <OPTION SELECTED VALUE="">-Selectionnez votre destination-</option>
                        <option value="dirigeant">Dirigeant</option>
                        <option value="societe">Societe</option>
                    </SELECT>
                </FORM>

        
           
        </div>
    </div>
</div>
</div>

        <script language="JavaScript">
            function ChangeUrl(formulaire)
            {
            if (formulaire.ListeUrl.selectedIndex != 0)
                {
                location.href = formulaire.ListeUrl.options[formulaire.ListeUrl.selectedIndex].value;
                }
            }
        </script> 
        
{% endblock %}

