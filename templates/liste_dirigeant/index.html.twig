{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<style>
tr{
    
    padding:auto;
    margin-right :20px;
}
       body {
        background-color: #FFEBEE
    }
    
    h1 {
        
        text-align:center;
    }
    
  td{
      width:100px;
  }
    
    .card {
        /* width: 400px; */
        background-color: #fff;
        border: none;
        border-radius: 12px;
        max-width: 600px;
        margin-left: 400px;
    }
    
    .action{
        text-align:center;
    }
   
    
    .card-body {
        padding:10px;
    }
    .nom{
        margin-left:10px;
    }
    .boutons  {
        margin-right : 20px;
        color:White;
    }
    a{
        color:black;
        text-decoration:none;
    }

</style>

    <h1> LISTE DES DIRIGEANTS</h1>

        
          
    <table class="table table-hover">
            <thead>
                <tr>
                <th scope="col" class="nom">Nom</th>
                <th scope="col">Prénom</th>
                <th scope="col">Sexe</th>
                <th scope="col">Adresse mail</th>
               
                </tr>
            </thead>
            {% for dirigeant in listedirigeants %}
                <tbody>
                    <tr class="table-danger">
                        <td class=e"nom">{{dirigeant.nom}}</td>
                        <td>{{dirigeant.prenom}}</td>
                        <td>{{dirigeant.sexe}}</td>
                        <td>{{dirigeant.mail}}</td>
                        <td>
                             <button class="btn btn-primary" type="submit" id="boutons"><a href="{{ path('edit_dirigeant', {
                            id: dirigeant.id }) }}">MODIFIER</a></button>
                              <button class="btn btn-primary" type="submit" id="bouton"><a href="{{ path('delete_dirigeant', {
                            id: dirigeant.id }) }}">SUPPRIMER</a></button>
                        </td>
                    </tr>
                
                </tbody>
            {% endfor %}
    </table>
            


{% endblock %}
